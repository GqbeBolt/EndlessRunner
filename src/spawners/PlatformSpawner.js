class PlatformSpawner {
    constructor(scene, platGroup) {
        this.scene = scene;
        this.platGroup = platGroup;

        // predef platforms as containers

        // adding all predefs to an array
        this.preDefs = [];

    }

    spawnNew() {
        
    }
}